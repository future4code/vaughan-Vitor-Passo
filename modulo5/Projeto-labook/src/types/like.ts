export type likeInfoDTO = {
  id: string;
  token: string;
};

export type deslikeInfoDTO = {
  id: string;
  token: string;
};

export type deslikingPostDTO = {
  post_id: string;
  user_id: string;
};
